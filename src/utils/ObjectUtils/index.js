import forEach from "./forEach";
import map from "./map";
import filter from "./filter";
import reduce from "./reduce";
import every from "./every";
import some from "./some";

export { forEach, map, filter, reduce, every, some };
